{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm run build:my-app",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "devCommand": "pnpm run dev",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
